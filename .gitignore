.DS_Store
node_modules/
.env
.env.*
!.env.*.example
!.env.example

# Strapi data exports/backups — never belong in source control,
# encrypted or not. Keep these in a proper backup location instead.
*.tar.gz.enc
